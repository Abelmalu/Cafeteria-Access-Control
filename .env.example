# --- Server Settings ---
SERVER_PORT=8080
# Use http://localhost:8080 for local dev or your server IP for production
BASE_URL=http://localhost:8080

# --- Database Settings ---
DB_TYPE=mysql
# Set DB_HOST to 'db' when running in Docker, or 'localhost' for local go run
DB_HOST=db
DB_PORT=3306
DB_NAME=cafeteria
DB_USER=root
DB_PASSWORD=your_database_password_here

# --- Storage Settings ---
UPLOAD_DIR=/app/uploads
PUBLIC_PREFIX=uploads/